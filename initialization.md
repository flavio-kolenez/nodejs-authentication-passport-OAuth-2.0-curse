## Iniciando o projeto

Comandos para facilitar a vida:

```bash
# Instale as dependencias do projeto:
npm install

# Builda o container:
docker-compose up -d --build

# Depois, caso queira somente subir o container:
docker-compose up -d
```

---